# Accepted exceptions for `npm run design:check` (tools/qa/scan.mjs --check).
# One per line: <file path suffix> :: <substring of the flagged line>   and a reason in a comment above.
# Every entry is a deliberate exception named in docs/DESIGN.md. Do not add one to make a screen pass.

# the ground: cream with two washes (DESIGN.md, Material)
styles/base.css :: radial-gradient(1100px 620px
styles/base.css :: radial-gradient(900px 560px
styles/base.css :: radial-gradient(940px 720px
# liquid glass is defined once, here (film, blur, the specular edge and top highlight, the coral film)
styles/base.css :: backdrop-filter
styles/base.css :: inset 0 1px 0 var(--glass-rim)
styles/base.css :: linear-gradient(to bottom, var(--glass-highlight-film)
# the nav lens: a see-through film with the specular edge, sliding under the active tab
app/nav.css :: inset 0 1px 0 var(--glass-rim)
# the Confirm tick floats over content: the sheet's shadow plus the specular edge on its disc and label plate
ui/confirm.css :: var(--sh-sheet), inset 0 1px 0 var(--glass-rim)
# the toast is glass: film, blur, edge
ui/toast.css :: inset 0 1px 0 var(--glass-rim)
ui/toast.css :: linear-gradient(to bottom, var(--glass-highlight-film)
# the sheet and the toast layer over content
ui/sheet.css :: backdrop-filter
# the sheet is liquid glass: the specular edge and the top-40% highlight
ui/sheet.css :: inset 0 1px 0 var(--glass-rim)
ui/sheet.css :: linear-gradient(to bottom, var(--glass-highlight-film)
ui/toast.css :: backdrop-filter
# the segmented thumb and the select option sit 3-4px inside a 12px control
ui/segmented.css :: calc(var(--r-control) - 3px)
ui/select.css :: calc(var(--r-control) - 4px)
# a person's identity dot: an inset ring in their hue, not a drop shadow
ui/fdot.css :: inset 0 0 0 1px color-mix
# the ship bobbing on the sea: one of the three authored motions
features/home/sea.css :: ship-bob
# the Wrapped certificate is glass over the drifting backdrop
features/wrapped/wrapped.css :: backdrop-filter
features/wrapped/wrapped.css :: inset 0 1px 0 var(--glass-rim)
features/wrapped/wrapped.css :: linear-gradient(to bottom, var(--glass-highlight-film)
# the Wrapped backdrop: the story's one ambient gradient and drift
features/wrapped/wrapped.css :: radial-gradient(circle at 74% 16%
features/wrapped/wrapped.css :: linear-gradient(160deg
features/wrapped/wrapped.css :: wr-drift
# TEMPORARY. The sheet's film is 88%, not the 58% tokens.css carries: a sheet opens over the sea
# hero, where 58% leaves 13px --ink-2 at 3.10:1 over --sea-lo. The sheet shadows the token locally
# until tokens.css and DESIGN.md carry 88%; delete this line with the shadow.
